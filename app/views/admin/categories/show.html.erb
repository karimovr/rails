<p id="notice"><%= notice %></p>

<p>
  <strong>Title:</strong>
  <%= @category.title %>
</p>

<%= link_to t('common.edit'), edit_admin_category_path(@category) %> |
<%= link_to t('common.back'), admin_categories_path %>
<% if current_user && current_user.admin? %>
    <%= link_to 'Все категории', admin_categories_path %>
<% end %>